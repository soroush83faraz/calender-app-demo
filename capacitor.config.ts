import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.soroush83faraz.calenderappdemo',
  appName: 'Calender App Demo',
  webDir: 'dist'
};

export default config;
